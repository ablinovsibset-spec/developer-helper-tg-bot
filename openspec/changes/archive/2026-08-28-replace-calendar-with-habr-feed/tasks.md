## 1. Скилл habr-feed.md (Тип A)

- [x] 1.1 Создать `skills/habr-feed.md` по образцу `skills/wttr-in-api.md`: назначение (правила получения топ-ленты Habr через `exec`), верифицированная команда из design.md (Раздел 2) с требованием выполнять её как есть, без модификаций
- [x] 1.2 Включить в скилл правила: `-L` обязателен (301-редирект), `--max-time 15`, один повтор после паузы 2–3 с при таймауте/5xx с последующей пометкой о недоступности, срез не более 10 статей (лента отсортирована по рейтингу — порядок сохранять), UTM-хвосты ссылок уже чистятся командой
- [x] 1.3 Включить правило вывода: пользователю — заголовок статьёй HTML-ссылкой `<a href="…">Заголовок</a>` (в Telegram включён `parse_mode=HTML`), спецсимволы `&`, `<`, `>` в заголовках экранировать

## 2. Рутина morning.md (Тип B)

- [x] 2.1 Заменить шаг 2 «События дня» на «Топ-статьи Habr за сутки» по правилам секции `habr-feed` (по образцу ссылки шага 1 на `wttr-in-api`); полностью удалить инструкции про `icalBuddy`/`khal` и оговорки про «календарь не подключён»
- [x] 2.2 Обновить шаг «Итоговая сводка»: после погоды — список свежих статей (не более 10) кликабельными заголовками; при недоступности Habr — пометка в сводке (как для погоды); предел 4000 символов и остальные требования сводки не меняются

## 3. README

- [x] 3.1 Обновить раздел «Скиллы»: три скилла вместо двух, описание `habr-feed.md` (Тип A — правила RSS-ленты Habr) и новой последовательности рутины в `morning.md`

## 4. Проверки

- [x] 4.1 Выполнить one-liner из скилла вручную: убедиться, что вывод — список «дата | заголовок | ссылка» без UTM, объём менее 3000 символов
- [x] 4.2 Запустить `pytest`: все тесты зелёные без правок тестов (тесты не завязаны на реальный состав `skills/`)
- [x] 4.3 Ручной smoke: перезапустить бота, запросить утреннюю сводку, убедиться, что в ответе есть погода и кликабельные статьи Habr за последние сутки
- [x] 4.4 Прогнать `openspec validate --change replace-calendar-with-habr-feed`

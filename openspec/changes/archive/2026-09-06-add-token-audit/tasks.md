# Tasks: add-token-audit

## 1. Телеметрия: разбивка промпта по ролям

- [x] 1.1 Добавить колонку `prompt_roles TEXT` в `_SCHEMA` (llm_calls) и
  idempotent-миграцию `ALTER TABLE` по `PRAGMA table_info` в
  `TelemetryStore.open()` (design D1/D2)
- [x] 1.2 Считать разбивку символов по ролям в `ObservingClient.complete()`,
  добавить поле в `LLMCallRecord` и INSERT (рядом с `prompt_chars`)
- [x] 1.3 Юнит-тесты: миграция старой базы добавляет колонку и не трогает
  записи; свежая база создаётся сразу с колонкой; разбивка по ролям в записи
  вызова; отсутствие ролей → null

## 2. Benchmark-харнесс

- [x] 2.1 Каркас `scripts/obs-benchmark.py`: сборка конвейера из main.py без
  Telegram — system prompt, своя MemoryStore, RunRecorder/ObservingClient,
  run_agent с SandboxExecutor; CLI `--db`, `--memory-db`, фильтр сценариев
  (design D3/D4)
- [x] 2.2 Фикстура `scripts/fixtures/sample_project/` — небольшой
  python-проект с модулями и тестами; доставка в `/work` через служебный
  exec + `docker cp` с пересозданием (design D5)
- [x] 2.3 Сценарии реалистичного микса: файловая задача над фикстурой,
  команда с объёмным выводом, поиск по истории, многоходовая сессия из 3+
  сообщений в одной сессии памяти (спека benchmark-harness)
- [x] 2.4 Обработка недоступности LLM/Docker: понятная ошибка, ненулевой
  exit code, без частичных прогонов; фиксированная метка прогонов
- [x] 2.5 Прогнать харнесс на реальном LLM (LM Studio, gpt-oss-20b) и
  убедиться: прогоны в отдельной БД с меткой, боевая база не тронута,
  `prompt_roles` заполнены

## 3. Аудит-скрипт

- [x] 3.1 Каркас `scripts/obs-audit.py`: stdlib sqlite3, `--db/--label/--since`,
  пустая база/нет файла — понятное сообщение (design D6)
- [x] 3.2 Секция Q1 «Топ инструментов»: агрегат output_tokens по tool_name,
  доли, ASCII-бары
- [x] 3.3 Секция Q2 «Самый дорогой ход»: распределение input_tokens по
  turn_number, ход с наибольшим средним, динамика роста
- [x] 3.4 Секция Q3 «Рост типов контекста»: доли типов по маппингу ролей
  (design D8), тренд первого→последнего хода, «недоступно» для записей без
  prompt_roles
- [x] 3.5 Секция Q4 «Повторные токены»: repeated/new по формуле Σ input(i<N),
  межпрогонный слой как оценку сверху с пометкой (design D7)
- [x] 3.6 Смоук-тесты скрипта на временной БД с синтетическими данными:
  все четыре секции, фильтры, tolerance к null-полям

## 4. Аудит и baseline

- [x] 4.1 Прогнать полный бенчмарк, снять отчёт `obs-audit.py` по метке
  харнесса
- [x] 4.2 Зафиксировать отчёт «до» (файл в task/ или рядом с бенчмарк-БД):
  цифры четырёх вопросов — baseline для Части 3

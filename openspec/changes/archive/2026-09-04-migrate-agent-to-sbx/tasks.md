# Tasks: migrate-agent-to-sbx

## 1. Спайки (закрывают Open Questions из design.md)

- [x] 1.1 Установить `sbx` CLI, выполнить `sbx login`; исследовать `sbx create --help` и определить позиционный шаблон для «голой» VM без агент-сессии (кандидат — базовый ubuntu-подобный шаблон); результат зафиксировать в design.md
- [x] 1.2 Определить способ поднятия остановленного сандбокса без attach к агент-сессии (`sbx start`? CLI reference/дашборд); проверить фактическое поведение env у `sbx exec -d` (доходят ли `-e` и хранимые переменные)
- [x] 1.3 Проверить достижимость LLM-хоста из сандбокса: `sbx policy allow network localhost:1234`, затем `sbx exec <name> curl http://host.docker.internal:1234/v1/models` — ответ LM Studio

## 2. Рефакторинг lifecycle исполнителя (design D5)

- [x] 2.1 Перенести владение `SandboxExecutor` из `run_agent` в `main`: старт в startup-последовательности, проброс в хендлеры, best-effort `stop()` в `finally` главного цикла; `run_agent` принимает готовый executor
- [x] 2.2 Сменить семантику контейнера на долгоживущего жителя: create при первом exec после старта бота, без удаления между сообщениями; самовосстановление (пересоздание + повтор команды один раз) сохранить
- [x] 2.3 Обновить `SANDBOX_ENV_LINE` в `skills.py`: файлы и пакеты переживают сообщения, сброс — только пересозданием контейнера/сандбокса

## 3. Hermetic-тесты (без Docker, фейки)

- [x] 3.1 Обновить тесты агентного цикла и main под новую передачу executor: отсутствие per-message start/stop, один executor на процесс бота
- [x] 3.2 Текст системного промпта отражает персистентное состояние окружения

## 4. Docker-интеграционные тесты (маркировка `docker`)

- [x] 4.1 Файл, созданный при обработке одного сообщения, читается следующим сообщением; `/new` не затрагивает файловое состояние
- [x] 4.2 Пакет `pip install --user` доступен в последующих сообщениях без переустановки
- [x] 4.3 Контейнер-житель не удаляется между сообщениями, удаляется при завершении бота; sweep сирот работает после аварийного завершения
- [x] 4.4 Смерть жителя посреди работы: пересоздание с чистым состоянием, повтор команды, бот не падает

## 5. Runbook-скрипты (design D7)

- [x] 5.1 `scripts/sbx-setup.sh`: создание сандбокса с workspace (шаблон из спайка 1.1), `python3 -m venv ~/.venv-devbot`, editable-установка пакета; подсказки про login и network policy
- [x] 5.2 `scripts/sbx-start.sh`: поднятие остановленного сандбокса (способ из спайка 1.2), фоновый запуск бота с `-e LLM_BASE_URL=http://host.docker.internal:1234/v1`, логи в `/tmp/bot.log`, хвост лога в выводе
- [x] 5.3 Сценарий обновления кода: остановка процесса бота внутри VM (`pkill`) + повторный `sbx-start.sh` — правки с хоста применяются

## 6. Документация

- [x] 6.1 README: требования (sbx CLI, login, policy), сценарии запуска (подготовка / ежедневный запуск / обновление / пауза / полный сброс), обновлённое описание песочницы (персистентное состояние, ручное восстановление после ребута)

## 7. Опробование миграции (спека sbx-hosting, сценарии)

- [x] 7.1 Полный прогон на реальном сандбоксе: setup → start → диалог с ботом: ответ получен, файл переживает сообщения, `cat <workspace>/.env` инструментом exec недоступен, скиллы с интернетом работают

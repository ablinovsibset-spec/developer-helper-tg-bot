## 1. Дефолты в пакете

- [x] 1.1 `config.py`: `DEFAULT_BASE_URL = "https://routerai.ru/api/v1"`, `DEFAULT_MODEL = "openai/gpt-5.6-luna"`
- [x] 1.2 `config.py`: переформулировать docstring виртуального прайса так, чтобы 0.11 / 0.60 читались как учётная величина, а не как цена новой дефолтной модели (design D6)
- [x] 1.3 Убедиться, что каскад `make_embeddings()` остаётся прежним (`EMBEDDING_* → LLM_* → дефолт`) — правок не требует, только проверка

## 2. Интерактивный выбор при запуске

- [x] 2.1 `sbx-start.sh`: сменить `DEFAULT_LLM_BASE_URL` на `https://routerai.ru/api/v1` и `DEFAULT_LLM_MODEL` на `openai/gpt-5.6-luna`
- [x] 2.2 `sbx-start.sh`: добавить четвёртый промпт `EMBEDDING_MODEL` с дефолтом `baai/bge-m3`, порядок — URL, chat-модель, embedding-модель, ключ
- [x] 2.3 `sbx-start.sh`: дефолт ключа брать из `.env` точечным извлечением строки `LLM_API_KEY=`, без `source` файла (design D3)
- [x] 2.4 `sbx-start.sh`: приоритет для ключа — экспортированное окружение, затем `.env`, затем пусто; для URL и модели — окружение, затем константа
- [x] 2.5 `sbx-start.sh`: ввод `-` на запросе ключа даёт пустой ключ (design D4)
- [x] 2.6 `sbx-start.sh`: добавить `-e "EMBEDDING_MODEL=…"` в вызов `sbx exec`; `EMBEDDING_BASE_URL` / `EMBEDDING_API_KEY` не передавать (design D5)
- [x] 2.7 `sbx-start.sh`: обновить головной комментарий скрипта (четыре параметра, откуда берётся дефолт ключа, что `-` очищает ключ)
- [x] 2.8 `sbx-start.sh`: проверить, что `say` перед запуском печатает URL и обе модели, а про ключ — только `set` / `empty`

## 3. Тесты

- [x] 3.1 `test_config.py`: обновить утверждения о литеральных значениях `DEFAULT_BASE_URL` и `DEFAULT_MODEL`
- [x] 3.2 `test_config.py`: добавить тест, что при незаданном `EMBEDDING_MODEL` и заданном `LLM_MODEL` клиент эмбеддингов получает `baai/bge-m3`, а не chat-модель
- [x] 3.3 `test_config.py`: добавить тест, что при незаданных `EMBEDDING_BASE_URL` / `EMBEDDING_API_KEY` клиент эмбеддингов берёт chat-endpoint и chat-ключ
- [x] 3.4 Прогнать `pytest` (герметичный набор, docker-тесты исключены)

## 4. Документация

- [x] 4.1 `.env.example`: обновить дефолты `LLM_BASE_URL` / `LLM_MODEL` в комментариях и пояснить, что `EMBEDDING_BASE_URL` / `EMBEDDING_API_KEY` нужны только для отдельного поставщика эмбеддингов
- [x] 4.2 README, таблица переменных: новые дефолты `LLM_BASE_URL` и `LLM_MODEL`
- [x] 4.3 README, раздел ежедневного запуска: четыре промпта, новые дефолты, откуда берётся дефолт ключа, сброс ключа через `-`, и что Enter×4 — это облачный (платный) стек
- [x] 4.4 README: пример локального LM Studio переписать из «дефолт» в «ввод с клавиатуры»
- [x] 4.5 README, раздел «Эмбеддинги»: наследование URL и ключа от чата и независимый выбор модели при запуске
- [x] 4.6 `AGENTS.md`: поправить устаревшую gotcha про `sbx-start.sh` — теперь четыре интерактивных параметра, включая `EMBEDDING_MODEL`, и дефолт ключа из `.env`

## 5. Проверка на живом боте

- [x] 5.1 `scripts/sbx-start.sh` с Enter×4: бот отвечает в Telegram, загрузка документа и поиск по нему работают
- [x] 5.2 Запуск со вводом `-` на ключе: убедиться, что ключ из `.env` в процесс не попал

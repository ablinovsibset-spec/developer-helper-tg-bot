## 1. Спайк

- [x] 1.1 Проверить, отдаёт ли LM Studio `usage.prompt_tokens_details.cached_tokens`: один живой запрос, результат зафиксировать заметкой для отчёта (design D7)

## 2. Инструмент read_file

- [x] 2.1 Спецификация READ_FILE_TOOL_SPEC (path, offset, limit; нумерация строк, потолок, подсказка продолжения) и правка описания exec: файлы читать read_file, не cat (specs/read-file-tool, design D1)
- [x] 2.2 Реализация read_file через CommandExecutor той же песочницы: обрезка до 1500 симв., «продолжай со строки N», ошибки текстом без прерывания цикла
- [x] 2.3 Подключить read_file в execute_tool_call и наборы инструментов main.py и obs-benchmark.py
- [x] 2.4 Unit-тесты: диапазон строк; без offset/limit; обрыв потолком с подсказкой; файл не найден; файл, созданный через exec, читается

## 3. Компакция tool-выводов

- [x] 3.1 Бюджет 8000 симв. на tool-сообщения прогона в run_agent: при превышении старейшие сворачиваются в заглушку «[компакция: вывод обрезан, было N симв.]»; user/assistant не трогаются (specs/agent-loop, design D2)
- [x] 3.2 Unit-тесты: превышение бюджета сворачивает старейшие; в рамках бюджета ничего не меняется; диалог не компактируется

## 4. Дата/время и шов сборки контекста

- [x] 4.1 build_system_prompt без даты/времени + единая функция сборки запроса: контекстная строка «Текущие дата и время: …» в начале текущего user-сообщения, после загруженной истории (specs/skills, design D3–D4)
- [x] 4.2 Применить шов в main.handle_text и Harness.process_message, убрав дублирование сборки
- [x] 4.3 Unit-тесты: строка присутствует и обновляется между сообщениями; префикс system+история байтово стабилен между сообщениями сессии и внутри прогона

## 5. Харнесс: метка и автопроверки

- [x] 5.1 Параметр --label в obs-benchmark.py со значением по умолчанию — текущая константа (specs/benchmark-harness MODIFIED)
- [x] 5.2 Автопроверки финальных ответов: verbose — 4 501 500; multiturn шаг 3 — три языка шага 1; files — упоминание бага; провал маркируется отдельно от статуса прогона (specs/benchmark-harness ADDED)
- [x] 5.3 Прогон харнесса вручную: метка пишется, проверки выводятся, провал виден

## 6. Аудит: кэш-скидка

- [x] 6.1 Эффективная стоимость в obs-audit.py: cached_tokens по отдельному CLI-прайсу (по умолчанию 0.25 × входного), рядом с сырой; при отсутствии кэш-данных — честная пометка (specs/token-audit, design D5)
- [x] 6.2 Тесты/проверка CLI: есть cached_tokens → обе стоимости; нет → равны с пометкой

## 7. Замер «после» и отчёт

- [x] 7.1 Три прохода харнесса с меткой optimized (21+ прогонов)
- [x] 7.2 Сравнение аудитов меток benchmark vs optimized: сырые входные токены, статусы прогонов, автопроверки; вывод о −30% и успехе (≤ −2%); при недостижении — вынести решение по резерву (спайк lazy skills) отдельно, не молча
- [x] 7.3 Отчёт до/после task/observability-3-otchet.md по образцу baseline-файла: цифры, обе стоимости, факт выбора read_file моделью, оговорки
- [x] 7.4 Обновить README: новый инструмент, метка харнесса, кэш-скидка аудита

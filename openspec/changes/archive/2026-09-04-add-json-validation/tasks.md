## 1. Контракт response_format

- [x] 1.1 Расширить протокол LLMClient (llm/base.py): опциональный параметр response_format в complete (design D5)
- [x] 1.2 openai_compat: проброс response_format в payload; деградация — HTTP 400 повторить без поля, факт неподдержки кэшировать до конца жизни процесса
- [x] 1.3 Тесты: response_format попадает в payload; 400 → повтор без поля и кэш; запрос без параметра не меняется

## 2. Обобщённый валидатор

- [x] 2.1 Вынести JSON-валидацию аргументов из execute_tool_call в обобщённый валидатор (ToolCall, ToolSpec) → ошибка|None: синтаксис + обязательные строковые параметры; текст ошибки содержит конкретную ошибку разбора и ожидаемую форму (design D2)
- [x] 2.2 Добавить точку расширения validate_final(content) → ошибка|None — пока всегда валидно (design D2)
- [x] 2.3 Тесты валидатора: корректный JSON; битый JSON с текстом ошибки; JSON без обязательного параметра; не-строковый параметр

## 3. Счётчик и терминалы в агентном цикле

- [x] 3.1 Счётчик подряд идущих провалов валидации (MAX_JSON_RETRIES = 3), сброс любым успешным ходом; терминальное сообщение о неспособности модели сформировать корректный структурированный ответ — отличимое от STEPS_EXHAUSTED (design D3)
- [x] 3.2 Обрыв по длине: невалидная структура И finish_reason == "length" → сразу терминальное сообщение об обрыве, без повторных запросов; валидная структура при length принимается (design D4)
- [x] 3.3 Тесты цикла на scripted-фейках LLM: три провала подряд → свой терминал; провал-успех-провал → счётчик обнулён; length без ретраев; фидбек содержит битый ответ и конкретную ошибку в истории

## 4. Проверка

- [x] 4.1 Полный тест-сьют зелёный (pytest), включая существующие тесты agent-loop без регрессий
- [x] 4.2 Ручная проверка на живой модели в LM Studio: замер, не подавляет ли json_schema tool-calling (сверка для будущего потребителя финального JSON; вывод зафиксировать в изменении)

# Tasks: pin-routerai-flex

## 1. Ретраи сетевых сбоев LLM-клиента (llm-provider)

- [x] 1.1 Ретраи в `OpenAICompatibleClient`: обрыв соединения/ответа,
      таймаут, 5xx и 404 — до 3 попыток суммарно, паузы 1с/3с; постоянные
      4xx (кроме 404) — исключение без повторов
- [x] 1.2 Юнит-тесты: успешный повтор после обрыва, исчерпание попыток,
      401/400 без ретраев, 404 с ретраем
- [x] 1.3 Проверить, что строка модели с `@...` уходит в запрос как есть
      (тест на payload)

## 2. Фактическая стоимость в телеметрии (agent-observability)

- [x] 2.1 Миграция `llm_calls`: nullable-колонка `billed_cost REAL`
      (идемпотентный ALTER по паттерну `prompt_roles`)
- [x] 2.2 Извлечение стоимости из usage ответа в `openai_compat`
      (число → значение, иначе null) и передача в Usage
- [x] 2.3 Запись `billed_cost` в `LLMCallRecord`/`insert_llm_call`,
      агрегат `SUM(billed_cost)` в `finish_run`
- [x] 2.4 Юнит-тесты: число/отсутствие/ноль в billed_cost, агрегат прогона,
      запись ошибочного вызова без стоимости

## 3. Аудит Q5 по фактическому биллингу (token-audit)

- [x] 3.1 Q5: при наличии `billed_cost` — фактическая сумма (₽) основной
      строкой, оценки по прайсу — следом с пометкой «оценка»; иначе —
      прежнее поведение
- [x] 3.2 Юнит-тесты трёх сценариев спеки: billed есть; billed нет, кеш
      есть; ни того ни другого

## 4. Конфигурация и документация

- [x] 4.1 `.env.example`: закомментированный блок routerai-flex
      (BASE_URL, MODEL с `@provider=openai/flex&allow_fallbacks=false`,
      KEY, flex-прайсы 11.26/67.54)
- [x] 4.2 README: раздел про routerai-экономику — тиры/цены, почему пин на
      flex, изменчивость кеша и надбавка за запись
- [x] 4.3 Отчёт до/после в `task/`: метки `routerai` vs `routerai-flex`,
      −47% по billed, методика и оговорки

## 5. Валидация

- [x] 5.1 Прогон тестов: `uv run pytest` зелёный
- [x] 5.2 Контрольный проход харнесса на routerai-flex: все вызовы billed
      по flex-ценам, автопроверки сценариев пройдены

## 1. Хранилище (memory.py)

- [x] 1.1 Добавить aiosqlite в зависимости (pyproject.toml, uv.lock)
- [x] 1.2 Реализовать MemoryStore в src/dev_helper_bot/memory.py: open/close, автосоздание файла, схема sessions/messages с индексами, режим WAL (design D1–D3)
- [x] 1.3 Методы записи и чтения: append_user, append_assistant, close_session (закрыть открытую сессию чата), load_open_history (хвост до 50 сообщений) (design D4, D5, D8)
- [x] 1.4 Метод search_completed: LIKE-поиск по завершённым сессиям чата, до 10 выдержек по ~300 символов с датой сессии; пустой результат — явный маркер (design D6)
- [x] 1.5 Тесты MemoryStore на tmp-файле: автосоздание, границы сессий при /new, восстановление после закрытия/переоткрытия (имитация рестарта), изоляция чатов, поиск (найдено/не найдено/лимит)

## 2. Интеграция в бота

- [x] 2.1 main.py: открыть MemoryStore при старте, закрыть в finally (паттерн песочницы); env MEMORY_DB_PATH со значением по умолчанию на VM-локальном диске (design D2, D7)
- [x] 2.2 handle_text: строить историю как [system] + load_open_history + новое user-сообщение; user писать в БД до агентного цикла, финальный ответ — после успеха (design D4)
- [x] 2.3 handle_new: закрывать сессию через close_session вместо обнуления списка
- [x] 2.4 Удалить ChatHistories и производный in-memory код из main.py
- [x] 2.5 Интеграционные тесты обработчиков: контекст переживает «рестарт» (новый store), транскрипт инструментов не попадает в БД и не восстанавливается, /new разделяет сессии

## 3. Инструмент search_history

- [x] 3.1 Добавить ToolSpec search_history (параметр query) и исполнение через MemoryStore.search_completed в общий путь инструментов агента
- [x] 3.2 Скилл-инструкция в skills/: когда модель должна искать по прошлым беседам (вопросы «что мы обсуждали…»)
- [x] 3.3 Тесты инструмента: найдено/не найдено/ограничение объёма/изоляция чатов/только завершённые сессии

## 4. Документация и проверка

- [x] 4.1 README: память диалога на SQLite, MEMORY_DB_PATH, живучесть (перезапуск бота и пауза сандбокса — да, полный сброс — нет), ручной бэкап через sbx exec
- [x] 4.2 Ручная проверка в сандбоксе: диалог → рестарт процесса → продолжение сессии; /new → вопрос о прошлой беседе → поиск находит; полный сброс → чистый старт без ошибок

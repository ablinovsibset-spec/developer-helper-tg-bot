# Tasks: minimal-agent

## 1. Эволюция шва LLM (tool-calling)

- [x] 1.1 Расширить `dev_helper_bot/llm/base.py`: опциональные ключи `Message` (`tool_calls`, `tool_call_id`), типы `ToolCall` и `AssistantTurn` (`content`, `tool_calls`, `finish_reason`)
- [x] 1.2 Переработать `OpenAICompatibleClient.complete(messages, tools=None) -> AssistantTurn`: проброс `tools` в payload, парсинг `tool_calls` (включая `arguments` как JSON-строку), сериализация истории с ролями `assistant`/`tool`; `reasoning` из ответа отбрасывается; дефолт таймаута 120с
- [x] 1.3 Обновить `config.py`: дефолт `LLM_MODEL=openai/gpt-oss-20b`
- [x] 1.4 Тесты: unit-контракт без `tools` возвращает ход без вызовов; integration на фейковом HTTP-сервере — round-trip `tool_calls` → история с `tool`-сообщением → финал; дефолты конфигурации

## 2. Инструмент exec

- [x] 2.1 Создать `tools.py`: JSON-схема инструмента `exec` (обязательный строкочный `command`) и async-исполнитель через subprocess shell
- [x] 2.2 Реализовать ограничения: таймаут с параметром (дефолт 30с) с принудительным завершением процесса; обрезка вывода ~3000 символов (голова ~1500 + хвост ~1500 + маркер обрезки); формат результата: код выхода, stdout, stderr, признак таймаута
- [x] 2.3 Тесты локальными командами без сети: успешная команда, ненулевой код выхода, длинный вывод обрезается, таймаут (короткий таймаут + `sleep`)

## 3. Агентный цикл

- [x] 3.1 Создать `agent.py`: цикл `complete(history, tools)` → выполнение `tool_calls` → append результатов → повтор; лимит 8 вызовов LLM; финал по отсутствию `tool_calls` (включая пустой `content`); сообщение об исчерпании шагов
- [x] 3.2 Обработка ошибок вызова: битый JSON в `arguments` или отсутствие `command` → tool-сообщение с текстом ошибки, цикл продолжается
- [x] 3.3 Тесты на fake-LLM: последовательность `tool_calls` → финал (результат инструмента в следующем запросе); вечные `tool_calls` → остановка на 8-м шаге с сообщением; финал без инструментов

## 4. Скиллы

- [x] 4.1 Создать `skills.py`: загрузка `skills/*.md` при старте, сборка системного промпта (`Reasoning: medium` + секции скиллов по имени файла)
- [x] 4.2 Написать `skills/wttr-in-api.md` (Тип A: форматы запросов wttr.in, компактный вывод, `?lang=ru`, ограничение частоты)
- [x] 4.3 Написать `skills/morning.md` (Тип B: погода Минск → события дня → сводка ≤4000 символов, ссылка на `wttr-in-api`)
- [x] 4.4 Тесты: загрузка из временного каталога (имена и содержимое в промпте), пустой/отсутствующий каталог не ломает старт

## 5. Память чата и Telegram-UX

- [x] 5.1 В `main.py`: хранилище `dict[chat_id, list[Message]]`, обработчик текста через агент с системным промптом и накоплением истории (assistant-ходы с `tool_calls`, tool-результаты; без `reasoning`)
- [x] 5.2 Зарегистрировать `Command("new")`-обработчик раньше `F.text`: сброс истории, короткое подтверждение, команда не попадает в историю и не запускает цикл
- [x] 5.3 Отправка «⏳ Готовлю ответ…» до старта цикла; финальный ответ — `send_chunked` новым сообщением; сообщение об исчерпании шагов
- [x] 5.4 Тесты: контекст сохраняется между сообщениями (инверсия бывшего no-memory); `/new` сбрасывает контекст без вызова LLM; ⏳-сообщение отправляется до финала

## 6. Конфигурация и документация

- [x] 6.1 Обновить `.env.example` (`LLM_MODEL=openai/gpt-oss-20b`) и при необходимости `requirements.txt`/`pyproject.toml` (новых зависимостей не планируется — проверить)
- [x] 6.2 Обновить `README.md`: агентный режим, `/new`, каталог `skills/`, требования к LM Studio с загруженной gpt-oss-20b

## 7. Финальная проверка

- [x] 7.1 Полный прогон `pytest` без сети и секретов; `openspec validate minimal-agent`
- [x] 7.2 Ручной smoke против живого LM Studio: утренняя сводка по скиллу `morning.md` (погода → попытка календаря → сводка), проверка `/new`

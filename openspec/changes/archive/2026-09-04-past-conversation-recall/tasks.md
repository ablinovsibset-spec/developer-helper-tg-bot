# Tasks: past-conversation-recall

## 1. Хранилище: обзор завершённых сессий

- [x] 1.1 Реализовать `MemoryStore.list_completed_sessions(chat_id)` по design D1/D5: одним SQL-запросом последние 10 завершённых сессий чата с датой начала, числом сообщений и превью первого user-сообщения (обрезка до 120 символов с маркером); пустой результат — явный маркер «Завершённых бесед в этом чате нет»
- [x] 1.2 Unit-тесты (tests/unit/test_memory.py): обзор завершённых сессий; открытая сессия не попадает; лимит 10 последних по времени; обрезка превью; пустой результат; изоляция чатов

## 2. Инструмент list_sessions

- [x] 2.1 Расширить протокол `HistorySearcher` (tools.py) методом `list_sessions() -> str` и добавить `LIST_TOOL_SPEC` без параметров (design D2)
- [x] 2.2 Реализовать `ChatHistorySearcher.list_sessions()` как делегирование в `MemoryStore.list_completed_sessions`
- [x] 2.3 Диспетчеризовать `list_sessions` в `execute_tool_call` (agent.py) рядом с веткой `search_history`, включая ответ при недоступном шве; добавить `LIST_TOOL_SPEC` в `AGENT_TOOLS` (main.py)
- [x] 2.4 Unit-тесты (tests/unit/test_agent.py, tests/unit/test_tools.py): вызов уходит в шов; результат шва возвращается модели; недоступный шов — ошибка текстом; спецификация валидируется `validate_tool_call`

## 3. Строка о памяти в системном промпте

- [x] 3.1 Добавить `MEMORY_ENV_LINE` в skills.py и включить в `build_system_prompt` после `SANDBOX_ENV_LINE` (design D3): прошлые беседы хранятся; при ссылках на прошлое — сначала обзор завершённых сессий, затем поиск по ключевому слову; не отвечать «не помню» без проверки инструментов
- [x] 3.2 Unit-тесты (tests/unit/test_skills.py): строка присутствует в промпте; присутствует при пустом каталоге скиллов
- [x] 3.3 Обновить `skills/search-history.md` под двухшаговый паттерн «обзор → поиск»

## 4. Логирование вызовов инструментов

- [x] 4.1 Добавить в цикл `run_agent` (agent.py) журналирование по design D4: `log.info` имя + исход (успех/ошибка выполнения), `log.warning` для вызовов, отклонённых валидацией; аргументы и содержимое результатов не логируются
- [x] 4.2 Unit-тесты (tests/unit/test_agent.py, caplog): успешный вызов записан; ошибка выполнения записана; отклонённый валидацией вызов записан с причиной; без вызовов записей нет

## 5. Сквозная проверка

- [x] 5.1 Прогнать полный набор тестов (`pytest`), убедиться в зелёном статусе и отсутствии регрессий `search_history`

# Tasks: add-docker-sandbox

## 1. Образ песочницы

- [x] 1.1 Создать Dockerfile образа песочницы: базовый Alpine, `curl`, `python3`, каталог `/work` с владельством uid 1000, `ENV HOME=/work` (см. design D8, ловушка HOME)
- [x] 1.2 Проверить руками в терминале: сборка образа, запуск контейнера с флагами харднинга из design D5, выполнение верифицированной команды скилла `habr-feed`, `apk add` под non-root

## 2. Модуль песочницы

- [x] 2.1 Создать модуль `sandbox`: `docker create` с ограничениями (`--memory 512m --pids-limit 256 --cap-drop ALL -u 1000 --rm --label dev-helper-bot.sandbox=true`), старт и id контейнера, best-effort `rm -f`
- [x] 2.2 Реализовать `exec` команды в песочнице: `docker exec -w /work <id> timeout <N> sh -c "<cmd>"` через async-subprocess, форматирование результата по существующему контракту (stdout/stderr/exit_code/признак таймаута по exit 143 от busybox-таймаута, обрезка)
- [x] 2.3 Реализовать async-страховку: `wait_for` с бюджетом «таймаут + ~10с», убийство локального docker-CLI через `killpg` по существующему паттерну
- [x] 2.4 Реализовать ленивое самовосстановление: при ошибке «No such container» пересоздать песочницу и повторить команду один раз (design D6)

## 3. Интеграция в агентный цикл

- [x] 3.1 Ввести шов исполнителя команд (инъектируемый, для тестов — двойник) в `tools.py`/`agent.py`, не меняя контракт инструмента для модели
- [x] 3.2 Встроить lifecycle-хук в `run_agent`: создание песочницы перед первым шагом, принудительное удаление в `finally` независимо от исхода цикла

## 4. Запуск бота

- [x] 4.1 Реализовать startup-последовательность в `main.py`: `docker info` → отказ с понятным русским сообщением при недоступном демоне (стиль `telegram_token()`)
- [x] 4.2 Реализовать sweep осиротевших песочниц при старте: `docker ps -q --filter label=dev-helper-bot.sandbox=true` → `docker rm -f`
- [x] 4.3 Реализовать ensure image: `docker image inspect`, при отсутствии — `docker build` из Dockerfile репозитория

## 5. Системный промпт

- [x] 5.1 Добавить в системный промпт строку об окружении: изолированный Alpine-контейнер, файлы живут до конца обработки сообщения, `cd`/env между вызовами не сохраняются

## 6. Тесты

- [x] 6.1 Перевести unit-тесты `exec` на двойник исполнителя: stdout/stderr/exit, обрезка, таймаут, видимость файла в пределах сообщения (спека `test-suite`)
- [x] 6.2 Зарегистрировать маркер `docker` в `pyproject.toml`, дефолтный deselect (`addopts = -m "not docker"`), убедиться что `pytest` зелёный без Docker
- [x] 6.3 Написать docker-интеграционные тесты (`pytest -m docker`): жизненный цикл (создание/удаление после цикла), файл между шагами, сброс состояния между сообщениями, таймаут внутри контейнера, самовосстановление после смерти контейнера, sweep при старте, сборка образа при отсутствии

## 7. Документация

- [x] 7.1 Обновить README: требование Docker, принцип песочницы (контейнер на сообщение, ограничения), запуск, `pytest -m docker`
- [x] 7.2 Задокументировать остаточные риски в README: bridge-gateway открывает хостовые сервисы на `0.0.0.0` (Ollama по умолчанию), диск без per-container квоты ограничен 30с таймаутом

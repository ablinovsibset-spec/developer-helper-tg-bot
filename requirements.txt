# Источник истины о зависимостях — pyproject.toml ([project].dependencies).
# Этот файл оставлен для удобной установки: pip install -r requirements.txt.
# При изменении зависимостей обновляйте оба файла согласованно.
aiogram>=3.0,<4.0
python-dotenv>=1.0
aiohttp>=3.9
aiosqlite>=0.20
sqlite-vec>=0.1.9
sqlean.py>=3.45
pypdf>=5.0
python-docx>=1.1
